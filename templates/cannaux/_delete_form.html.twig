<form method="post" action="{{ path('app_cannaux_delete', {'id': cannaux.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cannaux.id) }}">
    <button class="btn btn-warning mt-3">Delete</button>
</form>
